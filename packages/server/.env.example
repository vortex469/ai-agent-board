# Server port (default: 8080)
PORT=8080

# Bind to loopback by default. A non-loopback bind requires an intentional security boundary.
HOST=127.0.0.1

# Comma-separated list of allowed CORS origins.
ALLOWED_ORIGINS=http://localhost:8081,http://localhost:4175,http://localhost:4176

# HTTP Host allowlist used for WebSocket upgrades.
ALLOWED_HOSTS=localhost,127.0.0.1

# --- Database ---
# SQLite (default) — set DB_PATH to change the file location
DB_PATH=./data/agentboard.db

# PostgreSQL — set DATABASE_URL to use Postgres instead of SQLite
# DATABASE_URL=postgresql://agentboard:your_password@localhost:5432/agentboard

# --- Agent models (optional) ---
# COPILOT_MODEL=claude-opus-4-20250514
# CLAUDE_MODEL=claude-opus-4-20250514
# CODEX_MODEL=gpt-5.2-codex

# --- Local AI coding agent via DeepSeek Harness headless (optional) ---
# Keeps the Agent Board agent id "local-openai" while routing execution through DSH.
# DSH_LAUNCHER_PATH=/path/to/@deepseek-ai/dsh/lib/bin.js
# DSH_HOME=/root/.dsh
# DSH_PROFILE=headless
# LOCAL_OPENAI_DISPLAY_NAME=Local AI
# LOCAL_OPENAI_MODEL=Qwen R9700

# --- Authentication ---
# Set API_KEY to require Bearer token auth on all API and WebSocket requests.
# Leave unset for open access (local dev). Must match VITE_API_KEY on the client.
# API_KEY=
# Scoped Hermes integration token. Prefer storing only the SHA-256 digest.
# SERVICE_TOKENS=[{"sha256":"<sha256-of-token>","scopes":["projects:read","agents:read","orchestrations:create","orchestrations:read","orchestrations:message"]}]
# AGENT_BOARD_PUBLIC_URL=https://kanban.example.com

# --- Security ---
# Comma-separated allowed repo root paths for agent file access
# POSIX example:
# ALLOWED_REPO_ROOTS=/home/you/projects,/tmp
# Windows example:
# ALLOWED_REPO_ROOTS=D:\git,C:\Users\you,C:\Users\you\AppData\Local\Temp

# Default max agent execution time in milliseconds (default: 3600000 = 60 min)
# Individual tasks can override this from 1-240 minutes.
# AGENT_TIMEOUT_MS=3600000

# --- Worktree dependency provisioning ---
# Maximum concurrent `npm ci` installs for Board-managed task worktrees.
# AGENTBOARD_WORKTREE_DEPENDENCY_CONCURRENCY=2
# Minimum free space required on the worktree filesystem before installing (default: 8 GiB).
# AGENTBOARD_WORKTREE_MIN_FREE_SPACE_BYTES=8589934592
